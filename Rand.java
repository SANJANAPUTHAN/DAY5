package ArrayDemo;

public class Rand{
public static void main(String argv[]){
int iRand;
//iRand = (int)Math.random();
//System.out.println(iRand);
}
}
//type casting error occurs here because the conversion of double to int should be done implicitly
